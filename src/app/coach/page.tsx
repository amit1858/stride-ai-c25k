"use client";import { useState } from "react";
export default function Coach(){const [q,setQ]=useState('');const [a,setA]=useState('');return <div className="space-y-3"><h1 className="text-3xl font-bold">Coach</h1><textarea className="w-full border p-2" value={q} onChange={e=>setQ(e.target.value)} /><button className="rounded bg-black px-4 py-2 text-white" onClick={async()=>{const r=await fetch('/api/coach',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({messages:[{role:'user',content:q}]})});setA((await r.json()).message)}}>Ask</button><p>{a}</p></div>}
